# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Zensical documentation site output (uv run --group docs zensical build)
site/
.zensical/

# macOS
.DS_Store

# render-ldraw skill output (model snapshots + archived renders)
*.front.png
*.iso.png
*.top.png
previous/
*.obj
*.zip
triage_decisions.db
reviews_triage/
.mcp.json
.claude/settings.local.json

# legacy evaluation corpus binaries (now migrated into user-data storage
# by `legolization corpus`; kept only as a one-way migration source) + run
# output
data/corpus/meshes/
data/corpus/synthetic/
eval/runs/
eval/profiles/
eval/stablelego/
eval/datasets/

# default corpus evaluation output (legolization corpus collect/assemble/evaluate)
legolization-eval/

# external validation datasets fetched by scripts/fetch_datasets.py and
# scripts/fetch_omr.py. No trailing slash: this is a symlink to an external
# volume on at least one machine, and `datasets/` would not match a symlink.
# scripts/datasets.toml + datasets.lock.toml are the committed truth.
/datasets
