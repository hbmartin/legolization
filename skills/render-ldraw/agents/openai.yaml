interface:
  display_name: "Render LDraw"
  short_description: "Render an LDraw model or bundle to PNG views for visual inspection."
  icon_small: "./assets/icon-small.svg"
  icon_large: "./assets/icon-large.svg"
  brand_color: "#D92B2B"
  default_prompt: "Use $render-ldraw to render my model to images and check how it looks."
policy:
  allow_implicit_invocation: true
