# v5 pending measurements — circle back

All three deferred runs closed during v6 (2026-07-20), each in an
isolated git worktree; results recorded in ROADMAP.md's v6 WS-M entry:

1. **Mesh-kind baseline** — swept post-flip in a worktree;
   `eval/baselines/scorecard-mesh.json` committed (WS-E entry).
2. **spot@24 subassemblies cell** — 80/155 unstable → 72/176 with
   subassemblies; completes the U1 evidence table (5 of 6 improved).
3. **spot@24 v5-end profile** — 996 bricks / 176 steps (155 → 176 is
   the U1 trade); wall contended (4 concurrent jobs), excluded from
   regression claims per docs/performance-testing.md.

This file is kept as the pointer from the v5 program's notes; nothing
remains pending.
